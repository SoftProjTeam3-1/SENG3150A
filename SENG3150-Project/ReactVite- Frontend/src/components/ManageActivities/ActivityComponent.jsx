/*
    This component is used as an activity and in a list of activities.
    It will be used in the ManageActivities component to display a list of activities under a category.
*/

const ActivityComponent = () => {

    return(
        <div>
            <div>Me Activity in List</div>
        </div>
    );
}

export default ActivityComponent;